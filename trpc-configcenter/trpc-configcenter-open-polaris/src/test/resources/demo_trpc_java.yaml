trpc:
  plugins:
    config:
      polaris: #名字注册服务的远程对象
        namespace: xxx
        serverAddrs:
          - 127.0.0.1
        token: 123
        timeout: 5000
        configs:
          - group: group
            files:
              - application.properties